apiVersion: v1
kind: Service
metadata:
  name: {{ include "laravel-frankenphp.fullname" . }}
  labels:
    {{- include "laravel-frankenphp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laravel-frankenphp.selectorLabels" . | nindent 4 }}
